import unittest
from calculator_app import Calculator

class TestOperations(unittest.Testcase):
    def test_sum(self)
        calc = Calculator (10, 2)
        self.assertEqual(calc.get_quotient(), 0, "the division is wrong")

        if __name== "__main__":
            unitest.main()

import unittest
from calculator_app import Calculator

class TestOperations(unittest.Testcase):
    def test_sum(self)
        calc = Calculator (10, 2)
        self.assertEqual(calc.get_difference(), 8, "the Answer is wrong")

        if __name== "__main__":
            unitest.main()
